{
  "name": "nodeshop",
  "private": true,
  "devDependencies": {
    "lerna": "^5.1.8"
  },
  "scripts": {
    "local": "lerna run local --stream --parallel",
    "build": "lerna build"
  },
  "workspaces": [
    "packages/*"
  ]
}
